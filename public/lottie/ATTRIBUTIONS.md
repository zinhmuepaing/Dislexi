# Lottie animation attributions

Both animations in this directory are ORIGINAL works, hand-authored for this
project on 2026-07-18 (Bodymovin/Lottie JSON written directly, no external
assets). Licensed CC0 / public domain — no attribution required.

| File | Description | Colors |
|---|---|---|
| `star-pop.json` | Five-point star pulse + sparkle (quiz success) | `--hl` #FFD34D fill, `--ink` #22303F stroke, `--hl-strong` sparkle |
| `pointer-bounce.json` | Pen-blue pointer dot bouncing onto a highlight bar (landing hero) | `--pen` #2B6CB0 dot, `--hl` #FFD34D bar |

Rule: anything added here must be self-hosted, license-vetted (CC0 or
equivalent), and noted in this table. No runtime fetches from external hosts
(no service worker; strict self-containment).
